import React from 'react';

const Cart = ({select}) => {
    
    return (
        <div>
            <ol>
                <li>{select}</li>
            </ol>
        </div>
    );
};

export default Cart;